[tools]
# Popcorn 0.2.x requires exactly these versions.
erlang = "26.0.2"
elixir = "1.17.3"

# After switching, re-enable Popcorn in mix.exs:
#   {:popcorn, "~> 0.2", only: :dev, runtime: false}
# Then:
#   mix deps.get && mix popcorn.cook
